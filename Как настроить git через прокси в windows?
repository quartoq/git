Как настроить git через прокси в windows?
В любой из файлов gitconfig записать адрес прокси-сервера
[http]
	proxy = https:/хост:порт
для удаления его просто удалить из файла

то же самое через командную строку
git config http.proxy https:/хост:порт

удалить
git config --unset http.proxy

область видимости как обычно система (--system - для всех пользователей), пользователь (--global), каталог (без опции)

