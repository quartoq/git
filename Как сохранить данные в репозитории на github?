Как сохранить данные в репозитории на github?

создаём через интерфейс github новый репозиторий
запускаем например git-bash.exe
заходит в каталог проекта, если там нет репозитория делаем его git init
добавляем файл в проект
добавляем его в область подготовленных файлов (в индекс), в данном случае все файлы
git add *
коммитим
git commit -m "first commit"
сохраняем изменение в репозитории на github
git push https://github.com/quartoq/имя_репозитория_можно_без_расширения_.git имя_ветки_например_master
ввводим логин и пароль, если они не запомнены в настройках git
должно сохраниться

git pull https://github.com/quartoq/имя_репозитория 
